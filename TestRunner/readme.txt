Novacom Test Runner
===================

This is a quick and dirty program for running the test cases in the 
Palm.Novacom.Tests namespace. When run it displays an interactive shell 
of sorts that allows you to enter numbers to run a specific test. The 
interface should be self-explanatory.
